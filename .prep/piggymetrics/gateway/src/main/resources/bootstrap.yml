spring:
  application:
    name: gateway
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      enabled: false

---

spring:
  profiles: development
  cloud:
    config:
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
      enabled: true